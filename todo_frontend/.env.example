# PUBLIC_INTERFACE
# React frontend environment variables for configuring backend API base URL.
# If not set, the app will default to '/api' which works when the frontend is served
# behind the same domain as the backend with a reverse proxy mapping /api.
REACT_APP_API_BASE_URL=/api
